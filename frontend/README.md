# CoreFlowFit Frontend (generated)

Instructions:

1. Copy this folder into your project or unzip.
2. Set VITE_API_URL in .env to https://coreflowfit-backend.onrender.com
3. Run `npm install` and `npm run dev`.
